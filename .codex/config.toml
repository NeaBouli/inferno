[agents]
max_threads = 6
max_depth = 1

[agents.spark_worker]
description = "Schneller Agent fuer kleine, klar definierte Codeaenderungen."
config_file = "./agents/spark-worker.toml"
nickname_candidates = ["Spark Patch", "Spark Scan", "Spark Test"]
