*** Begin Patch
diff --git a/scripts/docs-autosync.js b/scripts/docs-autosync.js
--- a/scripts/docs-autosync.js
+++ b/scripts/docs-autosync.js
@@
 console.log("[AutoSync] Running Docs AutoFixer...");
 execSync("node scripts/docs-autofixer.js", { stdio: "inherit" });

 console.log("[AutoSync] Running Docs Checker...");
 execSync("node scripts/docs-checker.js", { stdio: "inherit" });

 console.log("[AutoSync] Running Docs Indexer...");
 execSync("node scripts/docs-indexer.js", { stdio: "inherit" });

+// --- NEW: Run Docs Reporter at the end of the AutoSync chain ---
+console.log("[AutoSync] Running Docs Reporter...");
+try {
+  execSync("node scripts/docs-reporter.js", { stdio: "inherit" });
+  fs.appendFileSync(logFile, `[${new Date().toISOString()}] ✅ Docs Reporter executed via AutoSync.\n`);
+} catch (err) {
+  fs.appendFileSync(logFile, `[${new Date().toISOString()}] ❌ Docs Reporter failed: ${err.message}\n`);
+}
+
 console.log("[AutoSync] ✅ Completed full documentation sync.");
 fs.appendFileSync(logFile, `[${new Date().toISOString()}] ✅ AutoSync completed successfully.\n`);
*** End Patch
