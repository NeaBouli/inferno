*** Begin Patch
diff --git a/scripts/docs-autosync-alias.sh b/scripts/docs-autosync-alias.sh
--- a/scripts/docs-autosync-alias.sh
+++ b/scripts/docs-autosync-alias.sh
@@
 #!/bin/bash
 # 🪶 Inferno Docs AutoSync (manual alias version)
 
 ROOT_DIR="/Users/test/Desktop/inferno"
 LOG_DIR="$ROOT_DIR/logs"
 LOG_FILE="$LOG_DIR/docs-autosync.log"
 
-mkdir -p "$LOG_DIR"
-touch "$LOG_FILE"
-
-echo "[`date -Iseconds`] <0001f9e9> Running Docs AutoSync (alias mode)..." | tee -a "$LOG_FILE"
-
-# Run main orchestrator
-node "$ROOT_DIR/scripts/docs-autosync.js" >> "$LOG_FILE" 2>&1
-
-echo "[`date -Iseconds`] ✅ Docs AutoSync completed (alias mode)." | tee -a "$LOG_FILE"
-
-exit 0
+echo "🪶 Starting Docs AutoSync alias..."
+
+# ensure log dir
+mkdir -p "$LOG_DIR"
+if [ ! -f "$LOG_FILE" ]; then
+  echo "🆕 Creating log file at $LOG_FILE"
+  touch "$LOG_FILE"
+fi
+
+echo "[`date -Iseconds`] <0001f9e9> Running Docs AutoSync (alias mode)..." | tee -a "$LOG_FILE"
+
+# run orchestrator, capture status
+if command -v node >/dev/null 2>&1; then
+  node "$ROOT_DIR/scripts/docs-autosync.js" >> "$LOG_FILE" 2>&1
+  STATUS=$?
+else
+  echo "[`date -Iseconds`] ❌ Node.js not found!" | tee -a "$LOG_FILE"
+  STATUS=1
+fi
+
+if [ $STATUS -eq 0 ]; then
+  echo "[`date -Iseconds`] ✅ Docs AutoSync completed (alias mode)." | tee -a "$LOG_FILE"
+else
+  echo "[`date -Iseconds`] ⚠️ Docs AutoSync exited with status $STATUS." | tee -a "$LOG_FILE"
+fi
+
+echo "🪶 Done. Check logs/docs-autosync.log for details."
+
+exit $STATUS
*** End Patch
