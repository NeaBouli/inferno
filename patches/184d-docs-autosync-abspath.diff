*** Begin Patch
diff --git a/scripts/docs-autosync-hook.sh b/scripts/docs-autosync-hook.sh
--- a/scripts/docs-autosync-hook.sh
+++ b/scripts/docs-autosync-hook.sh
@@
-LOG_DIR="logs"
-LOG_FILE="$LOG_DIR/docs-autosync.log"
-
-mkdir -p "$LOG_DIR"
-touch "$LOG_FILE"
-echo "[`date -Iseconds`] <0001f9e9> Running Docs AutoSync Hook..." >> "$LOG_FILE"
-
-# Lauf die AutoSync-Kette
-node scripts/docs-autosync.js >> "$LOG_FILE" 2>&1
-
-echo "[`date -Iseconds`] ✅ Docs AutoSync Hook completed." >> "$LOG_FILE"
-
-# 🔁 Fallback: wenn Hook manuell gestartet wird (z. B. durch CI oder Audit)
-if [ ! -L ".git/hooks/pre-commit" ]; then
-  echo "[`date -Iseconds`] ℹ️ Hook symlink missing – reinstalling..." >> "$LOG_FILE"
-  mkdir -p .git/hooks
-  ln -sf ../../scripts/docs-autosync-hook.sh .git/hooks/pre-commit
-  echo "[`date -Iseconds`] 🔗 Hook symlink recreated." >> "$LOG_FILE"
-fi
+LOG_DIR="$(git rev-parse --show-toplevel)/logs"
+LOG_FILE="$LOG_DIR/docs-autosync.log"
+
+mkdir -p "$LOG_DIR"
+touch "$LOG_FILE"
+
+echo "[`date -Iseconds`] <0001f9e9> Running Docs AutoSync Hook..." | tee -a "$LOG_FILE"
+
+# Lauf die AutoSync-Kette
+node scripts/docs-autosync.js 2>&1 | tee -a "$LOG_FILE"
+
+echo "[`date -Iseconds`] ✅ Docs AutoSync Hook completed." | tee -a "$LOG_FILE"
+
+# 🔁 Fallback: wenn Hook manuell gestartet wird (z. B. durch CI oder Audit)
+if [ ! -L ".git/hooks/pre-commit" ]; then
+  echo "[`date -Iseconds`] ℹ️ Hook symlink missing – reinstalling..." | tee -a "$LOG_FILE"
+  mkdir -p .git/hooks
+  ln -sf ../../scripts/docs-autosync-hook.sh .git/hooks/pre-commit
+  echo "[`date -Iseconds`] 🔗 Hook symlink recreated." | tee -a "$LOG_FILE"
+fi
*** End Patch
