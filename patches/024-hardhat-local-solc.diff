*** Begin Patch
diff --git a/hardhat.config.js b/hardhat.config.js
index e69de29..b1f7b30 100644
--- a/hardhat.config.js
+++ b/hardhat.config.js
@@
-require("@nomicfoundation/hardhat-toolbox");
-
-module.exports = {
-  solidity: "0.8.20",
-};
+require("@nomicfoundation/hardhat-toolbox");
+
+module.exports = {
+  solidity: {
+    compilers: [
+      {
+        version: "0.8.20",
+        settings: {
+          optimizer: {
+            enabled: true,
+            runs: 200,
+          },
+        },
+      },
+    ],
+  },
+  paths: {
+    artifacts: "./artifacts",
+    cache: "./cache",
+  },
+  networks: {
+    hardhat: {
+      allowUnlimitedContractSize: true,
+    },
+  },
+};
diff --git a/scripts/test_buyback.sh b/scripts/test_buyback.sh
new file mode 100755
--- /dev/null
+++ b/scripts/test_buyback.sh
@@
+#!/bin/bash
+set -e
+
+echo "🧹 Cleaning artifacts..."
+npx hardhat clean
+
+echo "🛠️ Compiling with local solc 0.8.20..."
+npx hardhat compile --show-stack-traces
+
+echo "🧪 Running BuybackVault tests..."
+npx hardhat test test/BuybackVault.test.js --show-stack-traces
+
+echo "✅ BuybackVault tests completed."
diff --git a/docs/README.md b/docs/README.md
index 3f5e3d1..b7f2e29 100644
--- a/docs/README.md
+++ b/docs/README.md
@@
 ## Testing
 
 To run tests locally:
 
 ```bash
 npx hardhat test
+## Offline Compiler Setup (solc-select)
+
+In restricted environments (no outbound HTTP), Hardhat cannot auto-download the Solidity compiler.
+Install and use a local compiler with solc-select:
+
+bash +# install solc-select +pip3 install solc-select + +# install Solidity 0.8.20 +solc-select install 0.8.20 + +# set default version +solc-select use 0.8.20 + +# verify installation +solc --version +# expected: 0.8.20+commit... +
+
+After this, Hardhat will use the local solc instead of downloading.
+
+Run the dedicated BuybackVault test script:
+
+bash +./scripts/test_buyback.sh +
diff --git a/logs/project.log b/logs/project.log
index 1c1a2b4..f39a2cd 100644
--- a/logs/project.log
+++ b/logs/project.log
@@
[2025-09-23 10:00] arch ADR – ADR-027 Projektzusammenfassung & Abschluss der Architekturphase dokumentiert
+[2025-09-24 23:30] dev5a TOOLING – Offline solc-select workaround added (Patch 024)
*** End Patch
