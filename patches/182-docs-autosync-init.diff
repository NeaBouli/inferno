*** Begin Patch
diff --git a/scripts/docs-autosync.js b/scripts/docs-autosync.js
new file mode 100755
--- /dev/null
+++ b/scripts/docs-autosync.js
@@
+#!/usr/bin/env node
+/**
+ * 🔁 Inferno Docs AutoSync
+ * Läuft alle Dokumentations-Skripte (Checker, Fixer, Indexer) automatisch in Folge
+ */
+
+const { execSync } = require("child_process");
+const fs = require("fs");
+const path = require("path");
+
+const LOG_FILE = path.join(__dirname, "..", "logs", "docs-autosync.log");
+if (!fs.existsSync(path.dirname(LOG_FILE))) fs.mkdirSync(path.dirname(LOG_FILE), { recursive: true });
+
+function log(msg) {
+  const line = `[${new Date().toISOString()}] ${msg}`;
+  console.log(line);
+  fs.appendFileSync(LOG_FILE, line + "\n");
+}
+
+const scripts = [
+  "node scripts/docs-checker.js",
+  "node scripts/docs-autofixer.js",
+  "node scripts/docs-indexer.js",
+];
+
+log("🔁 Starting Docs AutoSync...");
+for (const cmd of scripts) {
+  try {
+    log(`▶ Running: ${cmd}`);
+    execSync(cmd, { stdio: "inherit" });
+    log(`✅ Done: ${cmd}`);
+  } catch (err) {
+    log(`❌ Error in ${cmd}: ${err.message}`);
+  }
+}
+log("🧱 Docs AutoSync complete.");
*** End Patch
