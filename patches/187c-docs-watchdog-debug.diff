*** Begin Patch
diff --git a/scripts/docs-watchdog.js b/scripts/docs-watchdog.js
--- a/scripts/docs-watchdog.js
+++ b/scripts/docs-watchdog.js
@@
 const dirs = ["docs", "reports", "logs"];
 const cutoff = Date.now() - 24 * 60 * 60 * 1000; // 24h
 let outdated = [];
+
+console.log("[Watchdog] 🟡 Initialized at", new Date().toISOString());
+
+try {
+  fs.mkdirSync(logDir, { recursive: true });
+  console.log("[Watchdog] 📁 Log directory ready:", logDir);
+} catch (err) {
+  console.error("[Watchdog] ❌ Failed to create log dir:", err.message);
+}
@@
 if (outdated.length === 0) {
   console.log("✅ All docs fresh — no action required.");
   try {
     fs.appendFileSync(logFile, `[${new Date().toISOString()}] ✅ All docs fresh\n`);
   } catch {
     fs.mkdirSync(path.dirname(logFile), { recursive: true });
     fs.writeFileSync(logFile, `[${new Date().toISOString()}] ✅ Log initialized (fresh)\n`);
   }
   process.exit(0);
 }
+
+console.log("[Watchdog] ⚙️  Proceeding with outdated file check...");
*** End Patch
