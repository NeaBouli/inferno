*** Begin Patch
diff --git a/scripts/readme-autosync.js b/scripts/readme-autosync.js
new file mode 100755
--- /dev/null
+++ b/scripts/readme-autosync.js
@@
+#!/usr/bin/env node
+/**
+ * Inferno README Auto-Sync
+ * Scans docs/, logs/, patches/ for status icons and rebuilds README tree.
+ */
+
+import fs from "fs";
+import path from "path";
+
+const ROOT = process.cwd();
+const README = path.join(ROOT, "README.md");
+const DOCS = path.join(ROOT, "docs");
+
+function scanStatus() {
+  const status = { done: 0, open: 0, error: 0, doc: 0 };
+  const recurse = (dir) => {
+    for (const f of fs.readdirSync(dir)) {
+      const full = path.join(dir, f);
+      const stat = fs.statSync(full);
+      if (stat.isDirectory()) recurse(full);
+      else {
+        const txt = fs.readFileSync(full, "utf8");
+        if (txt.includes("🟢")) status.done++;
+        if (txt.includes("🔵")) status.open++;
+        if (txt.includes("🔴")) status.error++;
+        if (txt.includes("📘")) status.doc++;
+      }
+    }
+  };
+  recurse(DOCS);
+  return status;
+}
+
+function generateTree(status) {
+  return [
+    "```",
+    "# 🔰 Inferno Repository Tree",
+    "docs/          – Documentation  (" + status.doc + " 📘)",
+    "contracts/     – Smart Contracts",
+    "patches/       – Applied Patches",
+    "logs/          – Development Logs",
+    "scripts/       – Automation & Build",
+    "testing/       – Tests & QA",
+    "```",
+    "",
+    `🟢 Done: ${status.done} 🔵 Open: ${status.open} 🔴 Error: ${status.error} 📘 Docs: ${status.doc}`,
+  ].join("\n");
+}
+
+const content = fs.readFileSync(README, "utf8");
+const status = scanStatus();
+const tree = generateTree(status);
+
+const updated = content.replace(/```[\s\S]*?```[\s\S]*?(?=🟢|$)/, tree + "\n");
+fs.writeFileSync(README, updated);
+
+console.log("✅ README tree auto-synced.");
+
diff --git a/.github/workflows/readme-autosync.yml b/.github/workflows/readme-autosync.yml
new file mode 100644
--- /dev/null
+++ b/.github/workflows/readme-autosync.yml
@@
+name: README Auto-Sync
+
+on:
+  push:
+    branches: [main]
+
+jobs:
+  sync:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v4
+      - name: Setup Node
+        uses: actions/setup-node@v4
+        with:
+          node-version: 22
+      - name: Run README Auto-Sync
+        run: node scripts/readme-autosync.js
+      - name: Commit updated README
+        run: |
+          git config user.name "inferno-bot"
+          git config user.email "bot@inferno.system"
+          git add README.md
+          git commit -m "auto: sync README tree (Patch 162)" || echo "no changes"
+          git push
+
diff --git a/docs/CHANGELOG.md b/docs/CHANGELOG.md
--- a/docs/CHANGELOG.md
+++ b/docs/CHANGELOG.md
@@
 - [00:50] Dev111 INIT – Docs Dashboard Auto-Builder hinzugefügt (Patch 161)
+- [01:10] Dev112 INIT – README Status Auto-Sync Script + Workflow (Patch 162)
diff --git a/logs/project.log b/logs/project.log
--- a/logs/project.log
+++ b/logs/project.log
@@
 [2025-10-06 00:50] Dev111 INIT – Docs Dashboard Generator & Workflow integriert (📘)
+[2025-10-06 01:10] Dev112 INIT – README Auto-Sync Script & Workflow integriert (📘)
*** End Patch
