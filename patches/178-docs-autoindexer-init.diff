*** Begin Patch
diff --git a/scripts/docs-indexer.js b/scripts/docs-indexer.js
new file mode 100755
--- /dev/null
+++ b/scripts/docs-indexer.js
@@
+#!/usr/bin/env node
+/**
+ * 🧭 Inferno Docs Auto-Indexer
+ * Scans the /docs folder for .md files and generates an overview (_index.md)
+ */
+
+import fs from "fs";
+import path from "path";
+
+const DOCS_DIR = path.resolve("docs");
+const INDEX_FILE = path.join(DOCS_DIR, "_index.md");
+
+console.log("🧭 Generating docs index...");
+
+const files = fs
+  .readdirSync(DOCS_DIR)
+  .filter(f => f.endsWith(".md") && f !== "_index.md")
+  .sort();
+
+const content = [
+  "# 📚 Inferno Docs Index",
+  "",
+  "_Auto-generated by scripts/docs-indexer.js_",
+  "",
+  "## Files",
+  "",
+  ...files.map(f => `- [${f}](${f})`),
+  ""
+].join("\n");
+
+fs.writeFileSync(INDEX_FILE, content);
+
+console.log(`✅ Docs index created with ${files.length} entries → ${INDEX_FILE}`);
+
diff --git a/docs/README.md b/docs/README.md
--- a/docs/README.md
+++ b/docs/README.md
@@
 - `arch/decisions/` – Architecture Decision Records (ADR)
 - `patches/` – Repository Patch-History
+
+### Docs Indexer 📘
+- Automatisches Node-Script (`scripts/docs-indexer.js`)
+- Erstellt `_index.md` mit Übersicht aller Markdown-Dateien in `/docs/`
+- Status: 📘 dokumentarisch
+
diff --git a/docs/CHANGELOG.md b/docs/CHANGELOG.md
--- a/docs/CHANGELOG.md
+++ b/docs/CHANGELOG.md
@@
 - [2025-10-05 22:45] DEV127 ✅ – Audit AutoCommit Hook working, summary autogen confirmed
+- [2025-10-05 23:00] DEV128 ✅ – Docs Auto-Indexer INIT
diff --git a/logs/project.log b/logs/project.log
--- a/logs/project.log
+++ b/logs/project.log
@@
 [2025-10-05 22:45] DEV127 ✅ – Audit AutoCommit Hook working, summary autogen confirmed
+[2025-10-05 23:00] DEV128 ✅ – Docs Auto-Indexer INIT (docs/_index.md generator)
*** End Patch
