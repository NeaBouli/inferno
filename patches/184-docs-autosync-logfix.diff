*** Begin Patch
diff --git a/scripts/docs-autosync.js b/scripts/docs-autosync.js
--- a/scripts/docs-autosync.js
+++ b/scripts/docs-autosync.js
@@
 const { execSync } = require("child_process");
 const fs = require("fs");
 const path = require("path");
+
+// --- Logging setup ---
+const LOG_DIR = path.join(__dirname, "../logs");
+const LOG_FILE = path.join(LOG_DIR, "docs-autosync.log");
+fs.mkdirSync(LOG_DIR, { recursive: true });
+
+function log(msg) {
+  const line = `[${new Date().toISOString()}] ${msg}\n`;
+  fs.appendFileSync(LOG_FILE, line);
+  console.log(msg);
+}
+
+log("🧩 Running Docs AutoSync...");
@@
 try {
   console.log("🔄 Running Docs AutoFixer...");
   execSync("node scripts/docs-autofixer.js", { stdio: "inherit" });
   console.log("📚 Running Docs Indexer...");
   execSync("node scripts/docs-indexer.js", { stdio: "inherit" });
   console.log("✅ Docs AutoSync complete!");
+  log("✅ Docs AutoSync completed successfully.");
 } catch (err) {
   console.error("❌ Docs AutoSync failed:", err.message);
+  log(`❌ Docs AutoSync failed: ${err.message}`);
 }
*** End Patch
