*** Begin Patch
diff --git a/scripts/docs-dashboard-badges.js b/scripts/docs-dashboard-badges.js
@@
-const dashboardPath = path.join(root, "reports", "docs-dashboard.html");
-if (!fs.existsSync(dashboardPath)) {
-  console.error(`❌ Dashboard not found: ${dashboardPath}`);
-  process.exit(1);
-}
+// Try local reports first, then _site temp dirs
+let dashboardPath = path.join(root, "reports", "docs-dashboard.html");
+if (!fs.existsSync(dashboardPath)) {
+  const tmpBase = "/var/folders/mp";
+  const tmpCandidates = fs.readdirSync(tmpBase).filter(d => d.includes("inferno-site"));
+  let found = null;
+  for (const dir of tmpCandidates) {
+    const p = path.join(tmpBase, dir, "_site", "reports", "docs-dashboard.html");
+    if (fs.existsSync(p)) {
+      found = p;
+      break;
+    }
+  }
+  if (found) {
+    console.log(`⚙️  Using fallback dashboard path: ${found}`);
+    dashboardPath = found;
+  } else {
+    console.error(`❌ Dashboard not found in reports or temp directories.`);
+    process.exit(1);
+  }
+}
*** End Patch
