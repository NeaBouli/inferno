*** Begin Patch
diff --git a/.github/workflows/release-docs.yml b/.github/workflows/release-docs.yml
new file mode 100644
--- /dev/null
+++ b/.github/workflows/release-docs.yml
@@
+# 🧩 Inferno ReleaseDocs Generator
+# Automatische Erstellung einer Release-Dokumentation bei jedem neuen Tag oder Release
+
+name: ReleaseDocs Generator
+
+on:
+  push:
+    tags:
+      - 'v*.*.*'
+  release:
+    types: [published]
+
+jobs:
+  generate-release-doc:
+    runs-on: ubuntu-latest
+    steps:
+      - name: Checkout repository
+        uses: actions/checkout@v4
+
+      - name: Setup Node.js
+        uses: actions/setup-node@v4
+        with:
+          node-version: 22
+
+      - name: Generate release doc
+        run: |
+          TAG=${GITHUB_REF_NAME}
+          FILE="docs/releases/release-${TAG}.md"
+          mkdir -p docs/releases
+          echo "# 🚀 Inferno Release ${TAG}" > $FILE
+          echo "" >> $FILE
+          echo "**Date:** $(date '+%Y-%m-%d %H:%M:%S')" >> $FILE
+          echo "**Commit:** ${GITHUB_SHA}" >> $FILE
+          echo "**Triggered by:** ${GITHUB_ACTOR}" >> $FILE
+          echo "" >> $FILE
+          echo "### 📦 Changelog Summary" >> $FILE
+          echo "" >> $FILE
+          tail -n 10 docs/CHANGELOG.md >> $FILE
+          echo "" >> $FILE
+          echo "_Auto-generated by ReleaseDocs workflow._" >> $FILE
+
+      - name: Commit & Push generated docs
+        env:
+          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
+        run: |
+          git config user.name "inferno-bot"
+          git config user.email "bot@inferno.local"
+          git add docs/releases/
+          git commit -m "docs: auto-generate release docs for ${GITHUB_REF_NAME}"
+          git push origin main
+
diff --git a/docs/CHANGELOG.md b/docs/CHANGELOG.md
--- a/docs/CHANGELOG.md
+++ b/docs/CHANGELOG.md
@@
 - [23:55] Dev108 INIT – Docs Autobuilder Notifier via Webhook hinzugefügt (Patch 158)
+- [00:10] Dev109 INIT – ReleaseDocs Generator CI hinzugefügt (Patch 159)
diff --git a/logs/project.log b/logs/project.log
--- a/logs/project.log
+++ b/logs/project.log
@@
 [2025-10-05 23:55] Dev108 INIT – Docs Autobuilder Notifier via Webhook integriert (📘)
+[2025-10-06 00:10] Dev109 INIT – ReleaseDocs Generator Workflow implementiert (📘)
*** End Patch
