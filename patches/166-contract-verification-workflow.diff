*** Begin Patch
diff --git a/.github/workflows/verify.yml b/.github/workflows/verify.yml
new file mode 100644
--- /dev/null
+++ b/.github/workflows/verify.yml
@@
+# ✅ Inferno Contract Verification Workflow
+name: Verify Contracts
+
+on:
+  workflow_dispatch:
+
+jobs:
+  verify:
+    runs-on: ubuntu-latest
+    steps:
+      - name: 🧱 Checkout Repository
+        uses: actions/checkout@v4
+
+      - name: 🟡 Setup Node.js
+        uses: actions/setup-node@v4
+        with:
+          node-version: 22
+
+      - name: 📦 Install Dependencies
+        run: npm ci
+
+      - name: 🧩 Verify Contract on Etherscan
+        env:
+          ETHERSCAN_API_KEY: ${{ secrets.ETHERSCAN_API_KEY }}
+          CONTRACT_ADDRESS: ${{ secrets.LAST_DEPLOY_ADDRESS }}
+        run: node scripts/verify.js
+
diff --git a/scripts/verify.js b/scripts/verify.js
new file mode 100755
--- /dev/null
+++ b/scripts/verify.js
@@
+#!/usr/bin/env node
+/**
+ * Inferno Contract Verification Script
+ * Uses Hardhat's built-in verify plugin.
+ */
+
+import hre from "hardhat";
+
+async function main() {
+  const address = process.env.CONTRACT_ADDRESS;
+  if (!address) throw new Error("❌ CONTRACT_ADDRESS not provided.");
+
+  console.log(`🔍 Verifying contract at ${address} ...`);
+  await hre.run("verify:verify", {
+    address,
+    constructorArguments: [],
+  });
+
+  console.log("✅ Verification complete.");
+}
+
+main().catch((err) => {
+  console.error("❌ Verification failed:", err);
+  process.exit(1);
+});
+
diff --git a/docs/CHANGELOG.md b/docs/CHANGELOG.md
--- a/docs/CHANGELOG.md
+++ b/docs/CHANGELOG.md
@@
 - [02:20] Dev115 INIT – Auto-Deploy Workflow (Patch 165)
+- [02:40] Dev116 INIT – Contract Verification Workflow (Patch 166)
diff --git a/logs/project.log b/logs/project.log
--- a/logs/project.log
+++ b/logs/project.log
@@
 [2025-10-06 02:20] Dev115 INIT – Auto-Deploy Workflow (📘)
+[2025-10-06 02:40] Dev116 INIT – Contract Verification Workflow (📘)
*** End Patch
