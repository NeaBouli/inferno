*** Begin Patch
diff --git a/scripts/docs-publisher.js b/scripts/docs-publisher.js
new file mode 100755
--- /dev/null
+++ b/scripts/docs-publisher.js
@@
+#!/usr/bin/env node
+/**
+ * 🚀 Inferno Docs Publisher
+ * Baut und veröffentlicht Dokumentationsinhalte automatisch auf GitHub Pages.
+ */
+
+import fs from "fs";
+import path from "path";
+import { execSync } from "child_process";
+
+const root = process.cwd();
+const siteDir = path.join(root, "_site");
+const docsDirs = ["docs", "reports", "logs"];
+
+// 🔧 Clean + Build site folder
+if (fs.existsSync(siteDir)) fs.rmSync(siteDir, { recursive: true, force: true });
+fs.mkdirSync(siteDir);
+
+// 📂 Copy relevant folders
+for (const d of docsDirs) {
+  const src = path.join(root, d);
+  if (!fs.existsSync(src)) continue;
+  const dest = path.join(siteDir, d);
+  fs.cpSync(src, dest, { recursive: true });
+}
+
+// 🧾 Create simple index.html
+let html = `<html><head><meta charset="utf-8"><title>Inferno Docs Portal</title></head><body>`;
+html += `<h1>🔥 Inferno Documentation Portal</h1><ul>`;
+for (const d of docsDirs) {
+  if (fs.existsSync(path.join(siteDir, d)))
+    html += `<li><a href="./${d}/">${d}/</a></li>`;
+}
+html += `</ul><p>Generated on ${new Date().toISOString()}</p></body></html>`;
+fs.writeFileSync(path.join(siteDir, "index.html"), html);
+
+// 🚀 Deploy to gh-pages
+try {
+  execSync(`git checkout gh-pages || git checkout -b gh-pages`, { stdio: "inherit" });
+  execSync(`rm -rf *`, { stdio: "inherit" });
+  execSync(`cp -r ${siteDir}/* .`, { stdio: "inherit" });
+  execSync(`git add .`, { stdio: "inherit" });
+  execSync(`git commit -m "docs: publish to gh-pages [auto]" || true`, { stdio: "inherit" });
+  execSync(`git push origin gh-pages --force`, { stdio: "inherit" });
+  execSync(`git checkout main`, { stdio: "inherit" });
+  console.log("✅ Docs successfully published to GitHub Pages.");
+} catch (err) {
+  console.error("❌ Publish failed:", err.message);
+}
+
+// 📜 Log entry
+fs.appendFileSync(
+  path.join(root, "logs/project.log"),
+  `[${new Date().toISOString()}] Docs Publisher run complete → gh-pages branch\n`
+);
diff --git a/docs/CHANGELOG.md b/docs/CHANGELOG.md
--- a/docs/CHANGELOG.md
+++ b/docs/CHANGELOG.md
@@
 - [21:00] Dev189 INIT – Docs Consolidator (Monthly Report Generator) erstellt (📘)
+- [21:30] Dev190 INIT – Docs Publisher (GitHub Pages Deploy) erstellt (📘)
diff --git a/logs/project.log b/logs/project.log
--- a/logs/project.log
+++ b/logs/project.log
@@
 [2025-10-08 21:00] Dev189 INIT – Docs Consolidator (Monthly Report Generator) erstellt ✅
+[2025-10-08 21:30] Dev190 INIT – Docs Publisher (GitHub Pages Deploy) erstellt ✅
*** End Patch
