# Public CDP project identifier for a dedicated test project.
# Allowlist both http://localhost:3012 and http://127.0.0.1:3012 in the CDP portal.
# Vite binds the IPv4 loopback address and CDP matches origins exactly.
VITE_CDP_PROJECT_ID=
